export default function Flows(){
  return (
    <main style={{padding:20}}>
      <h1>Flows (Editor in sviluppo)</h1>
      <p>Editor per definire sequenze di attività per ruolo — prossimamente.</p>
    </main>
  )
}
